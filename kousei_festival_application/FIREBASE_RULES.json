{
  "rules": {
    "festivalBoothApplications": {
      ".read": "auth != null && auth.token.email == 'gm@kousei.local'",
      "$id": {
        ".write": "!data.exists() || (auth != null && auth.token.email == 'gm@kousei.local')"
      }
    }
  }
}
