{
  "rules": {
    ".read": false,
    ".write": false,
    "lockerMessages": {
      "$lockerId": {
        "$messageId": {
          ".write": "!data.exists() && newData.exists()",
          ".validate": "newData.hasChildren(['message','imageUrl','createdAt'])",
          "message": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 500" },
          "imageUrl": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 2000" },
          "createdAt": { ".validate": "newData.isNumber()" },
          "$other": { ".validate": false }
        },
        ".read": "auth != null && (auth.token.email == $lockerId + '@kousei.local')"
      }
    },
    "courageGame": {
      "rooms": {
        "$class": {
          ".read": "auth != null",
          ".write": "auth != null",
          "stages": {
            "$stage": {
              "private": {
                "$playerId": {
                  ".read": "auth != null && data.child('email').val() == auth.token.email"
                }
              }
            }
          }
        }
      }
    }
  }
}
